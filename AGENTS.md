# Project Rules

## Python Environment
- Use only the Conda environment named `openai-hacky` for this project.
- Activate it before running any project commands: `conda activate openai-hacky`.
- Do not use `base` or any other Conda/virtual environment in this repository.
